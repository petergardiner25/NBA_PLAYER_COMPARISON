import React from 'react';
import ReactLoading from 'react-loading';

const Loading = () => (
    <ReactLoading type={'spin'} color={'#000000'} height={'20%'} width={'35%'} />
);
 
export default Loading;